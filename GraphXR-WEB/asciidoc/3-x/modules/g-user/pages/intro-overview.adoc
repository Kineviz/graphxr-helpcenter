= GraphXR User Guide

https://www.kineviz.com/graphxr[GraphXR] is a visual analytics platform for exploring labeled property graphs. A GraphXR project is a virtual 3D workspace designed to accelerate exploration and foster collaborative discovery.

== The GraphXR Platform 

image::/v3/01_00_01_UserGuideIntro.png[] 

With GraphXR you can:

* Seamlessly import graph data from sources including:

** A graph database via GQL, Cypher, or visual no-code graph query. Supported databases include Google SpannerGraph, Memgraph, Puppygraph, Neo4j, and more.
** A relational database via SQL or Azure DB via Gremlin.
** CSV and JSON via drag and drop. 

* Navigate the graph efficiently in GraphXR's 3D graph environment.

* Model, transform, and simplify complex graphs iteratively through data mapping and powerful data transforms, and save your work to a project's views and its internal datastore. 

* Discover graph patterns through widely used graph algorithms for *Path Finding*, *Centrality*, and *Community Detection*.

* Create compelling graph visualizations using flexible layouts and geospatial mapping.
 
* Save, share, and export data views and the internal datastore.

* Securely collaborate via managed users, permissions, and data sources.

== Supported Environments

* WINDOWS, MAC OSX, AND LINUX

* CLOUD, PRIVATE CLOUD, AND ON-PREMISES DATA HOSTING.

* The GraphXR client runs best in Google Chrome. Compatibility with other browsers may vary.